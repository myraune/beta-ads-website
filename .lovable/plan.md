

# Update Ad Formats Order and Naming

## Changes

### 1. AdFormatsCarousel.tsx (homepage carousel)
Rename "Premium Experience" to **"Interactive"** and reorder the array to: Video, Snipe Banner, Side Bar, Rich Media, Poll, Interactive.

### 2. AdFormats.tsx (case studies page tabs)
Reorder the array to match: Video, Snipe, Side Bar, Rich Media, Poll, Interactive. This file already uses "Interactive" as the name, so only reordering is needed.

Both files use a static array, so the change is a simple reorder of the existing objects.

