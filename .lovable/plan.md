

# Remove Cookiebot Script and Announcement Bar

## Changes

1. **`index.html`** (line 100-101): Remove the Cookiebot script tag and its comment.

2. **`src/components/Layout.tsx`**: Remove the `AnnouncementBar` import and its usage.

3. **`src/components/sections/AnnouncementBar.tsx`**: Delete the file (no longer needed).

